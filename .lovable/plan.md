

# План: Кликабельные строки и карточки товаров ФВГ

## Что будет сделано

### 1. Создать файл данных `src/data/fvgProducts.ts`
Определить интерфейс `FvgProduct` и массив из 5 моделей (ФВГ-0,37 ... ФВГ-6,4) с артикулами формата `СЗПК.ФВГ.037`, `СЗПК.ФВГ.074` и т.д. Каждая модель включает: article, model, flow, area, dimensions, images (загруженное фото + чертёж).

### 2. Загрузить фото в `public/images/fvg-render-clean.png`
Скопировать загруженное изображение (фвг.png) как главное фото раздела и карточек товара.

### 3. Обновить `src/pages/GazoochistkaFvg.tsx`
- Заменить главное hero-фото на загруженное изображение (`fvg-render-clean.png`).
- Сделать строки таблиц «Технические характеристики» и «Габаритные размеры» кликабельными — при клике переход на `/product/{article}`.
- Добавить `useNavigate` и стилизацию строк (курсор, подсветка при наведении, артикул как ссылка).

### 4. Добавить блок ФВГ в `src/pages/Product.tsx`
По аналогии со скрубберами добавить обработку артикулов `СЗПК.ФВГ.*`:
- Галерея изображений
- Таблица характеристик (модель, производительность, площадь фильтрования, габариты, материал)
- Кнопка «Скачать спецификацию (PDF)» с формой контактов
- Кнопка «Добавить в КП»
- Хлебные крошки: Каталог → Газоочистка → ФВГ → артикул

### 5. Обновить каталог
В `src/data/catalog.ts` заменить плитку ФВГ на новое фото (`fvg-render-clean.png`).

## Затрагиваемые файлы
- **Новый**: `src/data/fvgProducts.ts`
- **Новый ассет**: `public/images/fvg-render-clean.png`
- **Изменения**: `src/pages/GazoochistkaFvg.tsx`, `src/pages/Product.tsx`, `src/data/catalog.ts`

