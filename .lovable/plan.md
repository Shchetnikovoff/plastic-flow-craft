

# План: Обновление раздела «Каплеуловители» по данным afxpro.ru

## Проблема
Таблица габаритных размеров серии КУ.1 (`ku1Data`) содержит неверные типоразмеры (1000, 1500, 2000…) вместо правильных (900, 1400, 2200…). Данные не совпадают с референсом. Также есть дублирование — две отдельные таблицы (габариты и продуктовая) для одной серии.

## Что будет сделано

### 1. Исправить данные `ku1Data`
Заменить 14 строк таблицы габаритных размеров КУ.1 на правильные данные с afxpro.ru:
- Размеры: 300, 500, 900, 1400, 2200, 3400, 5500, 6900, 8500, 10700, 13500, 17000, 22000, 27500
- Все значения D, A, B, V и массы — по референсу

### 2. Объединить две таблицы КУ.1 в одну
Убрать дублирующую продуктовую таблицу (`ku1ProductData`) — вся информация уже содержится в габаритной таблице. Оставить одну полную таблицу с моделями, расходом, размерами и массой.

### 3. Добавить раздел «Фото готовой продукции»
По аналогии с референсом — секция с 3 фотографиями готовых каплеуловителей (используем существующие hero-фото + добавим заглушки при необходимости).

## Затрагиваемый файл: 1
- `src/pages/GazoochistkaKapleuloviteli.tsx` — исправление данных, объединение таблиц

