

## План: Добавить характеристики пластика PPC

### Изменения

**`src/data/products.ts`** — добавить данные:
- Интерфейс `MaterialSpecs` с полями: рабочая температура, химическая стойкость, массив цветов (название, RAL, применение)
- Объект `materialSpecs` с данными для PPC:
  - Рабочая температура: от -10 до +100 °C
  - Химическая стойкость: кислоты, щёлочи, соли
  - Цвета: серый (RAL 7032) — внутри помещения, натуральный (RAL 9003) — внутри помещения, голубой (RAL 5012) — улица, УФ-защита

**`src/pages/Index.tsx`** — между секцией «Материалы» и таблицей добавить блок:
- Заголовок «Характеристики пластика»
- Сетка 2 колонки в стиле существующего блока характеристик (`bg-card`, `border`, `text-xs/sm`)
- Строки: рабочая температура, химическая стойкость
- Отдельный подблок «Доступные цвета» — список цветов с RAL-кодами и областью применения
- Блок показывается только если для `selectedMaterial` есть данные в `materialSpecs`

