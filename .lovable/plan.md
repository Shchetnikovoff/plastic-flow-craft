

## Plan: Make flanec image backgrounds white

Edit all 5 flanec images (`public/images/flanec-1.png` through `flanec-5.png`) using the AI image editing tool to replace their current backgrounds with clean white backgrounds, matching the style of the rastrub product images.

Each image will be processed with the instruction to make the background pure white while preserving the product render.

