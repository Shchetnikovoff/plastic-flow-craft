

## Plan: Increase logo size further

Change the logo height class in `src/components/Header.tsx` from `h-20` (80px) to `h-28` (112px).

**File:** `src/components/Header.tsx`, line 21 — change `className="h-20 w-auto object-contain"` to `className="h-28 w-auto object-contain"`.

